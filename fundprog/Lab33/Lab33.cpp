#include <iostream>
#include <string>
#include <vector>
#include <sstream>
using namespace std;

int main() {
    string inputText, s, Temp;
    int k, l, Start, end, 
    cout << "inputText: ";
    getline(cin, inputText);
    cout << "original text:" << inputText << endl;



    for 


    //// Разбиваем текст на слова
    //istringstream iss(inputText);
    //vector <string> words;
    //string word;

    //while (iss >> word) {
    //    // Проверяем длину слова и добавляем его, если оно не превышает 6 символов
    //    if (word.length() <= 6) {
    //        words.push_back(word);
    //    }
    //}

    //// Формируем отредактированный текст
    //string editedText;
    //for (const string& w : words) {
    //    editedText += w + " ";
    //}

    //// Выводим исходный и отредактированный тексты
    //cout << "original text: " << inputText << endl;
    //cout << "edited text: " << editedText << endl;

    //return 0;
}





